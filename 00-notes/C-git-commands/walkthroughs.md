# Appendix C: Git Commands — Walkthroughs

This appendix is a reference listing of Git commands grouped by purpose. It contains no walkthroughs — each command entry is a brief description pointing to where the command is used elsewhere in the book.
